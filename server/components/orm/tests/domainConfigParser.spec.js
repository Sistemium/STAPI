'use strict';

import domainConfigsParser from '../domainConfigsParser';
import assert from 'assert';
import path from 'path';

describe('domain configs parser', () => {

  it.skip ('should find all config files and make a map', (done) => {

  });
});
